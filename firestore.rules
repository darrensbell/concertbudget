rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are permissive and allow anyone to read and write to the database.
    // This is suitable for development but MUST be secured before moving to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}